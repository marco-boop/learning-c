// Compile the C code:

// gcc basics.c -o basics
// This command compiles hello.c and produces an executable named basics.

// Run the executable:
// After compilation, you can run the executable from the terminal.

// Example:

// ./basics

#include <stdio.h>

int main (void) {
    printf("\n Hello World \n");

    return 0;
}